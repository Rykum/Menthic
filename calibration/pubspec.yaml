name: oracle_calibration
description: Loop de calibração (Brier/BSS/Platt) do Project Oracle.
version: 0.1.0
publish_to: none
environment:
  sdk: ^3.8.0
dependencies:
  oracle_store:
    path: ../store
dev_dependencies:
  test: ^1.25.0
