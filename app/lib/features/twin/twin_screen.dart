import 'package:flutter/material.dart';

class TwinScreen extends StatelessWidget {
  const TwinScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('twin')));
}
