import 'package:flutter/material.dart';

class SimulateScreen extends StatelessWidget {
  const SimulateScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('simular')));
}
